name: 🤖 Hourly OpportunityHub Auto-Updater

on:
  schedule:
    # Runs at the top of every single hour (UTC)
    - cron: '0 * * * *'
  workflow_dispatch:  # Allows 1-click manual trigger anytime from GitHub Actions tab
  push:
    branches:
      - main
    paths:
      - 'automation/**'
      - '.github/workflows/auto-update.yml'

concurrency:
  group: opportunityhub-auto-update
  cancel-in-progress: false

permissions:
  contents: write

env:
  PYTHON_VERSION: '3.11'
  PYTHONIOENCODING: 'utf-8'
  PYTHONUNBUFFERED: '1'

jobs:
  update-and-sync:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      # ─── 1. Setup ───
      - name: 📥 Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - name: 🐍 Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: 'pip'
          cache-dependency-path: automation/requirements.txt

      - name: 📦 Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r automation/requirements.txt

      # ─── 2. Run Scraping & Dynamic README Sync Pipeline ───
      - name: 🚀 Run Multi-Tier Scraping & Dynamic Sync
        env:
          DRY_RUN: 'false'
          AUTO_COMMIT: 'false'
          SENDER_EMAIL: ${{ secrets.SENDER_EMAIL }}
          SENDER_PASSWORD: ${{ secrets.SENDER_PASSWORD }}
        run: |
          python -m automation.pipeline

      # ─── 3. Commit & Push Updated Data & README ───
      - name: 📤 Auto-Commit Live Changes to Repository
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git remote set-url origin https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git
          
          git add data/ README.md
          
          if git diff --staged --quiet; then
            echo "✅ Everything is already up-to-date — no new changes to commit."
          else
            DATE=$(date -u +'%Y-%m-%d %H:%M UTC')
            git commit -m "🤖 Auto-update: ${DATE} — live sync & dynamic README"
            git pull --rebase origin main || true
            git push origin main || echo "⚠️ Push skipped (possibly pushed by concurrent runner)"
            echo "✅ Live updates pushed to GitHub!"
          fi
